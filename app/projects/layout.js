export const metadata = {
  title: 'My Projects | Eve',
  description: 'Your story projects in Eve. Track scenes, characters, plot holes, and themes across every project you are working on.',
}

export default function ProjectsLayout({ children }) {
  return children
}
