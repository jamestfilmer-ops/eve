export const metadata = {
  title: 'Dashboard | Eve',
  description: 'Your Eve dashboard. Pick up where you left off, start a new project, or dive into the craft library.',
}

export default function DashboardLayout({ children }) {
  return children
}
